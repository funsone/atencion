<%= simple_form_for(ayuda, html: { class: 'form-horizontal' }) do |f| %>
  <% if ayuda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ayuda.errors.count, "error") %> prohibited this ayuda from being saved:</h2>

      <ul>
      <% ayuda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="col-md-6">
    <%= f.input :nombre%>
</div>
<div class="col-md-6">
    <%= f.association :departamento, :label_method => lambda { |owner| "#{owner.nombre}" }   %>
</div>




  <div class="col-md-12 text-center">
    <%= f.submit "Guardar", class: "btn btn-default" %>
      <%= link_to 'Regresar', ayudas_path, class: "btn btn-default" %>
  </div>
<% end %>
