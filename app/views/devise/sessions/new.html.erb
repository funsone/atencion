<div class="row">
      <div class="col-sm-12  form-box">
        <div class="form-top">
          <div class="form-top-left">
            <h3>Bienvenido al sistema FUNSONE NÓMINA.</h3>
            <p>Introduzca su email y contraseña para ingresar:</p>

          </div>
          <div class="form-top-right">
            <img src="/assets/funsone.png" width="44%" heigh="44%" alt="logo" />

          </div>
        </div>

        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: 'form-horizontal' }) do |f| %>
 <%= f.input :email, required: false, autofocus: true, class:"form-username form-control"  %>
  <%= f.input :password, required: false, autocomplete: "off", class: "form-username form-control" %>
  <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>


            <div class="form-actions">
              <%= f.button :submit, "Log in" %>
            </div>



        <% end %>

      </div>
</div>





<%= render "devise/shared/links" %>
